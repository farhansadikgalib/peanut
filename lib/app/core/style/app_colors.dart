import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors - Based on Logo Red
  /// Main brand color - vibrant red from logo (#E63946)
  static const primaryColor = Color(0xFFE63946);
  
  /// Secondary brand color - lighter red (#FF6B6B)
  static const secondaryColor = Color(0xFFFF6B6B);
  
  /// Accent color for highlights - deep red (#D62828)
  static const primaryAccentColor = Color(0xFFD62828);
  
  /// Default button color (matches primary)
  static const buttonColor = Color(0xFFE63946);
  
  // Text Colors
  /// Secondary text color - gray (#919199)
  static const textColor = Color(0xFF919199);
  
  // Surface Colors
  /// Background color for filter sections (#E6E6E6)
  static const filterSectionColor = Color(0xFFE6E6E6);
  
  // Basic Colors
  /// Pure black (#000000)
  static const black = Color(0xFF000000);
  
  /// Pure white (#FFFFFF)
  static const white = Color(0xFFFFFFFF);
  
  /// Light gray for borders and dividers (#E5E5E5)
  static const gray = Color(0xFFE5E5E5);

  // Splash Screen Colors
  /// Primary red button on splash screen
  static const splashBlueButton = Color(0xFFE63946);
  
  /// Dark button on splash screen (#2C3E40)
  static const splashDarkButton = Color(0xFF2C3E40);

  // Dark Mode Theme Colors
  /// Dark mode background color (#0F1419)
  static const darkBackground = Color(0xFF0F1419);
  
  /// Dark mode surface color for elevated elements (#1A1F25)
  static const darkSurface = Color(0xFF1A1F25);
  
  /// Dark mode card background (#1E2329)
  static const darkCard = Color(0xFF1E2329);
  
  /// Dark mode border color (#2D3339)
  static const darkBorder = Color(0xFF2D3339);
  
  /// Dark mode primary text color (white)
  static const darkTextPrimary = Color(0xFFFFFFFF);
  
  /// Dark mode secondary text color (#9CA3AF)
  static const darkTextSecondary = Color(0xFF9CA3AF);
  
  /// Dark mode muted/disabled text color (#6B7280)
  static const darkTextMuted = Color(0xFF6B7280);

  // Trading App Specific Colors
  /// Success/Profit color - green (#10B981)
  static const successGreen = Color(0xFF10B981);

  /// Danger/Loss color - red (#EF4444)
  static const dangerRed = Color(0xFFEF4444);

  /// Warning color - amber (#F59E0B)
  static const warningAmber = Color(0xFFF59E0B);

  /// Info color - cyan (#06B6D4)
  static const infoCyan = Color(0xFF06B6D4);

  /// Light background for input fields (#F8FAFC)
  static const inputBackground = Color(0xFFF8FAFC);

  /// Border color for input fields (#E2E8F0)
  static const inputBorder = Color(0xFFE2E8F0);

  /// Focus border color - primary red
  static const focusBorder = Color(0xFFE63946);

  /// Error color (#DC2626)
  static const errorColor = Color(0xFFDC2626);

  /// Light red background for cards and sections
  static const lightRed = Color(0xFFFFF0F0);

  /// Subtle gray for secondary elements (#71717A)
  static const subtleGray = Color(0xFF71717A);
}
